<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="56dp">

    <Button
        android:background="@drawable/statelist_item_drawable"
        android:id="@+id/subjectlist_button"
        style="?android:attr/borderlessButtonStyle"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />


    <TextView
        android:id="@+id/textview_subject_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:textColor="@color/color.gray.dark"
        android:gravity="left"
        android:textSize="16sp"
        android:layout_centerVertical="true"
        android:paddingLeft="72dp"
        android:text="Fachname" />

    <ImageButton
        android:backgroundTint="@color/color.gray.light"
        android:id="@+id/button_subject_info"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentLeft="true"
        android:layout_marginRight="16dp"
        android:padding="8dp"
        android:layout_centerVertical="true"
        android:scaleType="fitCenter"
        android:src="@drawable/icon_info"
        style="?android:attr/borderlessButtonStyle"/>



</RelativeLayout>